(ns java)

; todo letters before and after C take a look into whole namespace

;bases  Returns the immediate superclass and direct interfaces of c, if any

;bean  Takes a Java object and returns a read-only implementation of the map abstraction based upon its

; cast  Throws a ClassCastException if x is not a c, else returns x.

; class  Returns the Class of x

;class? Returns true if x is an instance of Class

; comparator  Returns an implementation of java.util.Comparator based upon pred.

; compare  Comparator. Returns a negative number, zero, or a positive number when x is logically 'less than', 'eq

; construct-proxy  Takes a proxy class and any arguments for its superclass ctor and creates and returns an instance of t