(ns namespace)

; https://clojuredocs.org/clojure.core/alias

; https://clojuredocs.org/clojure.core/all-ns

; create-ns  Create a new namespace named by the symbol if one doesn't already exist, returns it or the already